{
  "name": "fswd-m2-axios-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Starter repo — LU 2.2: build a configured Axios instance + service layer driven by a Vite env var.",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
